@model PlataformaCreditos.Models.SolicitudCredito

@{ ViewData["Title"] = "Detalle de Solicitud"; }

<h2>Solicitud #@Model.Id</h2>
<dl class="row">
    <dt class="col-sm-3">Monto Solicitado</dt>
    <dd class="col-sm-9">S/ @Model.MontoSolicitado.ToString("N2")</dd>
    <dt class="col-sm-3">Fecha</dt>
    <dd class="col-sm-9">@Model.FechaSolicitud.ToString("dd/MM/yyyy HH:mm")</dd>
    <dt class="col-sm-3">Estado</dt>
    <dd class="col-sm-9">@Model.Estado</dd>
    @if (Model.Estado == EstadoSolicitud.Rechazado)
    {
        <dt class="col-sm-3">Motivo Rechazo</dt>
        <dd class="col-sm-9">@Model.MotivoRechazo</dd>
    }
</dl>
<a asp-action="MisSolicitudes" class="btn btn-secondary">Volver</a>